const { genSortObject } = require('./genSortObject')
const { getBrowserInfo } = require('./getBrowserInfo')
const { getCountry } = require('./getCountry')
const { getIP } = require('./getIP')

module.exports = {
  genSortObject,
  getBrowserInfo,
  getCountry,
  getIP
}
