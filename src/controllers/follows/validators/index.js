const { validateCreateFollow } = require('./validateCreateFollow')
const { validateDeleteFollow } = require('./validateDeleteFollow')

module.exports = {
  validateCreateFollow,
  validateDeleteFollow
}
